# get-ip-by-domain

A simple Python script that resolves a domain name to its IP address.

## Usage

```bash
python main.py
```

Enter a domain name when prompted, and the script will print the corresponding IP address.

## Requirements

- Python 3
